Mean Functions API
==================
Instead, your program will want to use the single methods that sit inside of the ``src.Core.means`` module:

.. automodule:: src.Core.means
   :members:

   Instantiate this class with the three lengths *a*, *b*, and *c* of the sides of the triangle.  Once created,
   the object offers several methods:

